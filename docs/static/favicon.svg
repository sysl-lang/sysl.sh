<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" role="img" aria-label="sysl">
  <title>sysl</title>
  <!--
    The favicon is the wordmark's own "s", set in Iceland and reversed out of a
    solid tile. A four-letter wordmark is illegible at 16px and a thin outline
    on transparent disappears against a dark browser chrome, so the tile carries
    the colour and the glyph is white in both schemes.

    The glyph is placed at its natural size and centred: it is 300 by 350 in the
    face's own units, which leaves a margin on the tile without being scaled at
    all.

    `sysl-avatar.png` beside this file is the same mark as a raster, for the
    places that will not take an SVG — the GitHub organisation's picture is
    the one that needed it. It is drawn full-bleed rather than with this
    file's rounded corners, because GitHub rounds an avatar itself and two
    radii that disagree leave a transparent sliver at each corner. Rebuild it
    with:

        sed 's/rx="96"//' favicon.svg > /tmp/square.svg
        rsvg-convert -w 512 -h 512 /tmp/square.svg -o sysl-avatar.png
  -->
  <rect width="512" height="512" rx="96" fill="#6f1f9e"/>
  <g transform="translate(106 81)">
    <path fill="#ffffff" d="M300 0 239 60H70V140H210Q239 140 260 160Q280 181 280 210V280Q280 309 260 330Q239 350 210 350H0V290H210V200H70Q41 200 20 180Q0 159 0 130V70Q0 41 20 20Q41 0 70 0Z"/>
  </g>
</svg>
